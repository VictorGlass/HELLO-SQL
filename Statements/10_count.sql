--COUNT

/*
Contara todos los usuarios que tienen el valor de edad en su columna
, no mostrara a los que no tengan ese valor.
*/
SELECT COUNT(age) FROM users;

/*
Contara a todos los usuarios que estan en la tabla.
*/
SELECT COUNT(*) FROM users;
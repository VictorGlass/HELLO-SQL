/*
Selecciona todos los elementos de la tabla users,
osea todo su contenido.
*/
SELECT * FROM users;

/*
Selecciona solo la columna nombre de la tabla users.
*/
SELECT name FROM users;

/*
Selecciona solo la columna 'user_id' y la columna 'name'
de la tabla users.
*/
SELECT user_id, name FROM users;

